# ChainWatch Audit Cycle — 2026-06-07 10:00 UTC

**Mode:** Solo audit (DB unreachable from sandbox; delegate_task not available in this sandbox)
**Confidence:** High — full source code review performed, all 90 automated checks pass

---

## Executive Summary

This cycle delivers a concrete improvement: a new `/api/health/diagnostic` endpoint that directly addresses the DB connection debugging gap identified in previous audits. Also completed a thorough review of all signal thresholds and monitor configuration parameters.

**Tasks picked up:**
1. Task 27 (`write_tool`): Add diagnostic endpoint for startup debugging
2. Task 28 (`review_thresholds`): Review signal thresholds and monitor config

---

## 🔍 Audit Findings

### Critical: 0

No critical bugs found.

### Minor: 0

No minor findings. All previous findings remain resolved.

---

## ✅ Work Completed

### 1. New Endpoint: `GET /api/health/diagnostic`

**File:** `backend/main.py` (lines 2079-2215, +139 lines)
**Commit:** `c16abd2`

Seven independent diagnostic checks:

| # | Check | What it diagnoses |
|---|-------|-------------------|
| 1 | DATABASE_URL parse & DNS resolution | Is the hostname in DATABASE_URL resolvable? |
| 2 | DB connectivity (SELECT 1) | Can asyncpg connect through the pool? |
| 3 | TCP reachability (raw socket) | Is the port open? (independent of asyncpg) |
| 4 | Environment variable presence | Which required env vars are set? |
| 5 | Monitor state | Alive/dead, poll config, price cache freshness |
| 6 | Migration log summary | How many migrations applied, latest filename |
| 7 | System info | Hostname, Python version, CWD |

**Safety properties:**
- No secrets leaked (API keys as boolean presence only)
- No DB writes
- Every check wrapped in try/except
- TCP socket has 5s timeout
- Same auth model as `/api/health` (unauthenticated, read-only)

### 2. Thresholds Review

| Parameter | Current | Verdict |
|-----------|---------|---------|
| MIN_SIGNAL_USD_BTC | $10,000 | ✅ Keep — filters dust, captures meaningful whale txns |
| MIN_SIGNAL_USD_ETH | $5,000 | ✅ Keep — appropriate for ETH gas economics |
| MIN_SIGNAL_USD_SOL | $2,000 | ✅ Keep — lower floor matches SOL's cheaper txns |
| POLL_INTERVAL | 60s | ✅ Keep — good freshness/rate-limit balance |
| MAX_CONSECUTIVE_ERRORS | 5 | ⚠️ Consider 8 for better RPC outage tolerance — DEFERRED |
| WALLET_FETCH_TIMEOUT | 25s | ✅ Keep |
| MAX_CONCURRENT_WALLETS | 5 | ✅ Keep |
| DEDUP_INTERVAL | 5 min | ✅ Keep |
| Alert cooldown | 5 min | ✅ Keep |

---

## ✅ What's Good (Verified in This Cycle)

1. **All 90/90 audit checks pass** — up from 88 (new patterns in diagnostic endpoint detected and verified)
2. **Diagnostic endpoint is well-structured** — 7 independent checks, each with proper error handling
3. **No secret leakage** — API keys reported as boolean, DB password not exposed
4. **Thresholds are well-calibrated** — all signal minimums appropriate for their chains
5. **Monitor config is solid** — poll interval, concurrency, and timeout values are reasonable
6. **All previous findings remain resolved** — no regressions

---

## 🚨 Deployment Action Items (Carried Forward)

These are infrastructure/deployment issues, not code bugs:

| # | Issue | Fix |
|---|-------|-----|
| 1 | **DB unreachable** — `db.ok: false` | Check `DATABASE_URL` env var in Zeabur. Verify PostgreSQL service is running in same project. Use new `/api/health/diagnostic` to pinpoint failure mode. |
| 2 | **Monitor not alive** — cascade of DB failure | Fixed automatically when DB is restored. |
| 3 | **CRON_SECRET not set** — task queue returns 503 | Set `CRON_SECRET` env var in Zeabur dashboard. |

---

## Recommended Follow-up Tasks

1. **`audit_outputs`** — Query the new diagnostic endpoint and verify it correctly reports all failure modes
2. **`analyze_failures`** — Use diagnostic output to pinpoint DB connection failure (DNS vs TCP vs auth)
3. **`write_tool`** — Add startup-log endpoint to complement diagnostic with historical data

---

*Completed by Hermes (solo mode) — DB unreachable from sandbox, delegate_task not available.*
*Next cycle: query diagnostic endpoint, check if DB restored, verify endpoint output accuracy.*
